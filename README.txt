Common git commands:

git pull origin master -- used to download the remote repository on github.
git push origin master -- used to upload the local repository and merge with the remote repository on github.
git add * -- used to add all new files to the local rep
git commit -am "some message" -- used to update the local rep with the modified files